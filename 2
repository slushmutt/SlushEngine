#include <string>
namespace SlushEngine {
    class Debug{
        public:
            enum LogType{
                INFO,
                WARNING,
                ERROR
            };
            template<typename ...Args>
            void Log(std::string str, Args...args){

            }
    };
}
